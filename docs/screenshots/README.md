# Screenshots

Referenced by the repo's top-level `README.md`:

- `main-window.png` — the main Classic-layout window (Processor Information + Temperature Readings table)
- `cards-view.png` — Cards layout (per-core load tiles)
- `dashboard-view.png` — Dashboard layout (large CPU temperature readout over the load tiles)
- `mini-mode.png` — mini mode
- `overheat-protection.png` — the Overheat protection dialog (Options menu)
- `about-dialog.png` — Help → About, showing the app icon and the full detected/supported processor list
- `settings-general.png` — Settings → General tab
- `settings-display.png` — Settings → Display tab
- `settings-notification-area.png` — Settings → Notification Area tab (tray font weight + ° symbol options)
- `settings-taskbar.png` — Settings → Windows Taskbar tab
- `tray-icon.png` — the live tray icon and tooltip in the Windows notification area

PNG, no fixed size requirement — the README scales them for display.
